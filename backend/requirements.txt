# Core web framework
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0

# Validation & config
pydantic>=2.10,<3.0
pydantic-settings>=2.7,<3.0
python-dotenv>=1.0,<2.0

# Rate limiting (per-IP token bucket via slowapi)
slowapi>=0.1.9,<1.0

# LLM (Google Gemini, latest SDK)
google-genai>=1.0,<2.0

# Numerics
numpy>=2.0,<3.0

# Machine Learning (Multi-ML anomaly detection + risk classification)
scikit-learn>=1.5,<2.0
joblib>=1.4,<2.0
pandas>=2.2,<3.0

# Async HTTP client (Open-Meteo + NASA POWER)
httpx>=0.27,<1.0

# Testing
pytest>=8.0,<10.0
